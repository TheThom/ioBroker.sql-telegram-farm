const MENU = require('./menu.json');
const MYSQL = require('./mySql.json');

function prepareMenu(menu, parameters) {
	return [text, keyboard];
}

module.exports = { generateKeyboard, prepareMenu };


			//#region Machines
			case MENU.MACHINES._:

			//#endregion